{
  "name": "04",
  "packageManager": "yarn@3.1.1",
  "scripts": {
    "jest": "jest",
    "test": "jest --watch",
    "jest:init": "jest --init"
  },
  "devDependencies": {
    "jest": "^29.3.1"
  }
}
